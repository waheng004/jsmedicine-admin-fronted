# Todos.md

## 当前阶段：阶段 2 管理端接口对齐与通用页面联调（进行中）

### 阶段 1 登录闭环与项目基础（已完成）

- [x] Vue 3 + Vite 项目初始化。
- [x] 管理端登录页、记住帐号密码、自动登录、退出登录。
- [x] 路由守卫与登录态 Cookie 存储。
- [x] 首次提交并推送到远程仓库。

### 阶段 2 管理端接口对齐与通用页面联调（进行中）

- [x] 按管理端使用手册生成后台菜单与功能清单。
- [x] 接入当前后端 `api/api.json` 中已有的管理端列表、详情、新增、修改、删除、审核、配置、统计接口。
- [x] 调整左侧菜单为一级菜单点击展开子菜单。
- [x] 修复移动窄屏下菜单和页面上下排列的问题，保持左侧菜单、右侧内容布局。
- [x] 隐藏当前接口文档中不存在的新增、修改、删除按钮，避免误提示已有接口缺失。
- [x] 将通用列表字段标题和状态值改为中文展示，答疑列表显示用户和专家名称。
- [x] 按 GitHub 后端 `main/dev` 当前 `api/api.json` 重新核对管理端接口路径，撤掉仓库未提供的操作入口。
- [x] 修正审核请求字段为 `comment`、学员认证审核字段为 `rejectReason`，并对齐图书章节、知识库等 DTO 字段。
- [x] 按 Apifox 契约接入 `PUT /api/v1/admin/users/{id}` 修改用户信息，支持昵称、签名、状态、身份、学员绑定、地区、机构和执业类型。
- [x] 按 Apifox 契约接入学员新增、删除、批量删除，并将学员新增/修改字段切换为 `AdminStudentUpsertRequest` 字符串枚举契约。
- [ ] 继续细化各业务表单字段、枚举、校验和详情展示，以 Swagger 请求体和响应体为准。
- [x] 按更新后的接口文档接入管理端统一封面上传链路，课程、图书、资讯、播客、专题、直播、专家、知识库、首页内容改为三段式上传并回填稳定 `coverUrl`。
- [ ] 补齐权限菜单和按钮控制，基于 `/api/v1/auth/me` 返回的角色与权限处理。
- [ ] 按真实后端联调结果修正字段名、状态枚举和错误提示。
  - 当前联调结论：`GET /api/v1/admin/users`、`GET /api/v1/admin/users/{id}` 仍返回 `https://example.com/assets/...` 头像测试地址，不是文档要求的 `/api/v1/files/{id}/content` 稳定地址；浏览器实际加载结果 `naturalWidth=0`，图片已失效，需后端修正数据或返回链路。
  - 当前联调结论：`GET /api/v1/admin/statistics/exam-scores/papers?page=1&size=10` 直接返回 `500 INTERNAL_ERROR`，前端页面已按文档接口调用，需后端排查成绩统计数据或服务实现。
  - 当前联调结论：`POST /api/v1/admin/interaction/qa/questions/{id}/answers` 在测试数据下返回 `500 INTERNAL_ERROR`，当前请求体已按文档发送 `expertId/content/auditComment`，需后端排查答疑回复链路。

### 当前确认缺少或待后端确认的管理端接口

- [ ] 用户新增接口。
- [ ] 用户删除接口。
- [x] 学员导入接口。
- [x] 学员导出接口。
- [x] 基础数据接口：机构、执业类型。
- [x] 直播视频子资源接口。
- [ ] 用户签名、用户业务身份、学员结构化认证材料、机构/执业类型、资讯/播客标签、直播标签等新增字段需等后端仓库合入后再接入。

### 阶段 3 页面精细化与验收（未开始）

- [ ] 为账户、系统、课程、图书、内容、专家、题库等高频模块替换通用 JSON 弹窗，做成更接近手册的表格、表单和配置页。
- [ ] 统计模块增加更易读的指标卡和图表展示。
- [ ] 关键操作增加二次确认、加载态、空状态和失败态。
- [ ] 执行完整浏览器验收和 `npm run build`。
